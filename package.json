{
  "name": "sketchcalibur",
  "version": "1.0.0",
  "description": "AI-Powered Collaborative Whiteboard",
  "scripts": {
    "start:ml": "cd ml-backend && python -m uvicorn app.main:app --reload --port 8000",
    "start:backend": "cd backend && npm start",
    "start:frontend": "cd frontend && npm run dev",
    "start:all": "concurrently \"npm run start:ml\" \"npm run start:backend\" \"npm run start:frontend\"",
    "install:all": "cd ml-backend && pip install -r requirements.txt && cd ../backend && npm install && cd ../frontend && npm install",
    "test:ml": "cd ml-backend && python test_ml_system.py",
    "docker:up": "docker-compose up --build",
    "docker:down": "docker-compose down"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
